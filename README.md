
Installation
------------
Run the command:
```
apt install git-all
apt install curl
apt install ffmpeg
apt-get install nvidia-cuda-toolkit
python install.py --onnxruntime cuda-11.8 --skip-venv
```

Usage
-----
